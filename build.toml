[general]
name = "szl_receipt_attn"
version = 1
edition = 5
license = "Apache-2.0"
backends = ["cuda"]
source = "https://github.com/szl-holdings/szl-receipt-attn"

[general.hub]
repo-id = "SZLHOLDINGS/szl-receipt-attn"

# Triton JIT lives in torch-ext/. Do not set kernel.backend = "triton"
# (obsolete). This is a torch-noarch kernel: no AOT .cu / torch_binding.
[torch-noarch]
pyext = ["py"]
